#
# mon premier ctf Default variables
# 



# Let to 1 for LAN use.
# 
# Else, use Let's encrypt with web_server/traefik_acme,
# copy your certs manualy in acme.json
# Manually build and launch traefik with docker-compose up --build 
TRAEFIK_SELF_SIGNED_CERTS=1